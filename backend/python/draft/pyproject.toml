[project]
name = "draft"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dishka>=1.10.1",
    "faststream>=0.6.7",
]

[tool.uv.sources]
shared = { workspace = true }
