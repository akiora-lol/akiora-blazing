[project]
name = "draft"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "faststream>=0.6.7",
    "shared",
]

[tool.uv.sources]
shared = { workspace = true }
